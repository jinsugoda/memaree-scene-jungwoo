import { baseMarginSize } from '../../../../../styles/stylesheets/spacing';

export const container = {
    flex: 1,
    alignItems: 'center' as 'center',
    marginHorizontal: baseMarginSize,
    marginTop: baseMarginSize,
};
